import React from "react";
import TodoComponent from "../../Components/Todo";

const TodoContainer = () => {
  return (
    <React.Fragment>
      <TodoComponent />
    </React.Fragment>
  );
};

export default TodoContainer;

